export type CarType = {
  carTypeCode: string;
  carTypeName: string;
};

export type Product = {
  carTypeCode: string;
  carCode: string;
  carName: string;
  carPrice: number;
  carImgPath: string;
};
