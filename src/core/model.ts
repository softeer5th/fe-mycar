export type CarType = {
  carTypeCoed: string;
  carTypeName: string;
};

export type Product = {
  carTypeCode: string;
  carCode: string;
  carName: string;
  carModel: number;
  carImage: string;
};
